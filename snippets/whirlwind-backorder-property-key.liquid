{%- comment -%}
  Zero-width space line item property key for variant backorder messages.
  Shopify checkout renders properties as "Key: Value"; an invisible key
  avoids the "Backorder Message:" prefix while keeping the value visible.
{%- endcomment -%}
{%- assign whirlwind_backorder_property_key = '​' -%}
